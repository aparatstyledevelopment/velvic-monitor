# citation_discipline

`(question, fact_pack)` pairs. Assertions:
- Every numeric claim in the answer carries a `[ec_xxx]` citation.
- Every `[ec_xxx]` resolves to a valid `engine_call_id` from the fact pack.
- The narrative does not contain numbers absent from the fact pack.

Populated in Phase 2 alongside the chat orchestrator.
