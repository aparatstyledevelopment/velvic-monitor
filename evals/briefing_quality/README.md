# briefing_quality

Promptfoo fixtures for the briefing-card composer. Each fixture is a FactPack
JSON (deterministic input) plus expected qualitative properties on the
generated narrative.

Populated in Phase 1 alongside the briefing pipeline.
